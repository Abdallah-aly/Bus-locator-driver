import 'package:flutter/material.dart';

class RatingTap extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Hello rating tab'),
    );
  }
}
